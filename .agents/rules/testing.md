# Testing Standards

- **Rust**: Unit test all mathematical intervals in `crates/core/src/fsrs.rs` and database queries in `crates/db`. Run `cargo test --workspace`.
- **Frontend**: Test shared formatters, hooks, and compound UI states.
- **AI Contracts**: Validate that Gemini extraction mocks parse correctly against the JSON schema.
